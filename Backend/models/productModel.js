export default class Product {
  constructor({ id, name, description, price, brand, stock }) {
    this.id = id;
    this.name = name;
    this.description = description;
    this.price = price;
    this.brand = brand;
    this.stock = stock;
  }
}
